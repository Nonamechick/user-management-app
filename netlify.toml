[build]
command = "cd client && npm install && npm run build && cd ../functions && npm ci"
publish = "client/build"
functions = "functions"
[dev]
command = "cd client && npm start"
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200